{
  "name": "learn-ml",
  "version": "0.1.0",
  "private": true,
  "description": "Interactive machine learning sandbox and leveled tutorial in the spirit of learnGitBranching, built around the scikit-learn mental model.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vite": "^6.0.7",
    "vitest": "^2.1.8"
  }
}
